machine:
  ruby:
    version: 2.3.1

dependencies:
  pre:
    - rvm use ruby-2.2.4
    - gem install jekyll
    - gem install bundler

general:
  artifacts:
    - "./_site"

test:
  override:
    - bundle install
    - bundle exec jekyll build
