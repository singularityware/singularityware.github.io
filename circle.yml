machine:
  ruby:
    version: 2.3.1

dependencies:
  pre:
    - gem install jekyll
    - gem install bundler

general:
  artifacts:
    - "./_site"

test:
  override:
    - bundle install
    - bundle exec jekyll build
